kind: Service
apiVersion: v1
metadata: 
  name: frontend-service
  namespace: frontend
  labels:
    app: frontend
      
spec:
  type: NodePort
    
  selector:
    app: frontend

  ports:
    - protocol: TCP
      port: 80 
      targetPort: 5173
      nodePort: 30080   
