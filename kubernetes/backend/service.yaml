kind: Service
apiVersion: v1
metadata: 
  name: backend-service
  namespace: backend
  labels:
    app: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081   
